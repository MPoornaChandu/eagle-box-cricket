# Supabase Backend

The active schema currently remains at `../../supabase/schema.sql` for compatibility with the existing project documentation and setup.

Use these folders for future backend changes:

- `migrations/` for ordered SQL migrations
- `sql/` for reusable SQL snippets
- `seed/` for seed data or import notes
